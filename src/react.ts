// Client-side React exports — use in 'use client' components only.
// Import from 'cms-client/react' to avoid pulling convex/react into server bundles.

export { CMSProvider } from './provider'
export { useSection } from './hooks/useSection'
